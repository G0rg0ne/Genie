[project]
name = "genie"
version = "0.1.0"
description = "Deep search agent"
requires-python = ">=3.12"

dependencies = [
    "langchain-openai",
    "langgraph",
    "langchain-mcp-adapters>=0.1.0",
    "loguru",
    "python-dotenv",
    "langchain-tavily",
    "fastapi",
    "uvicorn[standard]",
    "pydantic-settings",
]